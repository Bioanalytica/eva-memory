# EVA Memory System - Environment Variables
# Copy to .env and fill in your values, or set in ~/.claude/settings.json "env" block

# ============================================================================
# Neo4j (REQUIRED)
# ============================================================================
EVA_NEO4J_URI=bolt://localhost:7687
EVA_NEO4J_USER=neo4j
EVA_NEO4J_PASS=changeme

# ============================================================================
# Storage
# ============================================================================
# Data directory for markdown logs, state files, queue, and backups
EVA_STORE_PATH=./data

# Root directory containing scripts/, hooks/, mcp-server/ (MCP/hooks only)
EVA_MEMORY_DIR=/path/to/eva-memory

# ============================================================================
# ChromaDB + Ollama (OPTIONAL — enables semantic search)
# ============================================================================
EVA_CHROMA_URL=http://localhost:8000
EVA_CHROMA_COLLECTION=  # UUID of your ChromaDB collection
EVA_OLLAMA_URL=http://localhost:11434
EVA_OLLAMA_MODEL=nomic-embed-text  # or snowflake-arctic-embed2

# ============================================================================
# MCP / Hooks Configuration
# ============================================================================
# Timeout for memory.py subprocess calls (milliseconds)
EVA_COMMAND_TIMEOUT_MS=10000

# Whether to inject standing instructions on session start
EVA_INJECT_INSTRUCTIONS=true

# ============================================================================
# Auto-Recall Tuning
# ============================================================================
# Token budget for auto-recall injection (approximate, 1 token ~ 4 chars)
EVA_RECALL_MAX_TOKENS=400

# Minimum importance level (1-10) for memories to appear in auto-recall
EVA_RECALL_MIN_IMPORTANCE=3

# Maximum number of memories injected per session start
EVA_RECALL_MAX_RESULTS=5

# ============================================================================
# Client Isolation
# ============================================================================
# Set a unique ID per client when multiple clients share the same EVA_MEMORY_DIR
# Namespaces state.json and SESSION-STATE.md to prevent conflicts
# EVA_CLIENT_ID=claude-code-main
